import { Table } from './models/table';
import { increment, random } from './utils/auto';

/**
 * @exports { Table, increment, random } This is the export of the Table class and the increment and random functions for automatic value generation in the schema.
 */

export { Table, increment, random };